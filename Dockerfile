# Use Python 3.7 or later as a base image

# Copy contents into image

# Install pip dependencies from requirements.txt (do not install individually)

# Set YOUR_NAME environment variable

# Expose the correct port

# Create an entrypoint